# --- Web server ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.12.5
httpx>=0.27.0
python-dotenv>=1.0.1

# --- RAG: vector DB + embeddings ---
chromadb>=0.5.5
sentence-transformers>=3.0.1

# --- PII masking (Indonesian NER + regex) ---
# Presidio + spaCy were dropped: xx_ent_wiki_sm mislabelled Indonesian legal
# text badly in both directions (leaked names, masked "Nilai"/"Denda"), and
# Presidio's remaining pattern recognizers exactly duplicated masker.py's own
# regex layer. transformers is declared explicitly now that masker.py imports
# it directly rather than relying on the sentence-transformers dependency.
transformers>=4.40.0
